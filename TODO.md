# TODO: Merge Resume AI & Builder into one

- [x] 1. Add "Builder" tab to `/resume-tools` page (integrate resume-builder functionality).
- [x] 2. Update `site-header.tsx` to show only one "Resume AI" link (remove ✨ Builder).
- [x] 3. Redirect `/resume-builder` → `/resume-tools` so old links don't break.
- [x] 4. Update dashboard quick link to point to `/resume-tools`.
- [x] 5. Run type check to verify (only pre-existing unrelated error in dashboard-tabs.tsx remains).
